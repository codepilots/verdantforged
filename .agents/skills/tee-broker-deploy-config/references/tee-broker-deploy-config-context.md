---
name: tee-broker-deploy-config-context
description: Stub for the historical SKILL.md content that was trimmed from the umbrella tee-broker-deploy-config. See the umbrella's other references for the current authoritative detail.
---

# tee-broker-deploy-config — historical context (superseded)

The original SKILL.md grew past the 100K-character tool limit. This file is
a stub for that history.

If you need the prior content, check one of these sources (in order):

1. The skill's git history under
   `~/.hermes/skills/tee-broker-deploy-config/SKILL.md` (or the curator
   backup at `~/.hermes/.curator_backups/`).
2. A recent `hermes curator show tee-broker-deploy-config` invocation.
3. The umbrella's other references — most of the prior material is now
   covered by:
   - `references/attestation-nemoclaw-image-digest.md` (Plan 2)
   - `references/attestation-verifier-build-notes.md` (verifier)
   - `references/worker-identity-gate.md` (worker stalls)

The current umbrella `SKILL.md` is the authoritative entry point; this file
exists only to flag that the prior body has been retired.
